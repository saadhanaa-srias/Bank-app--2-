var credit_balance=parseInt(current_balance_el.innerText) + parseInt(entered_amount_el)
    document.getElementById("credit").innerHTML;
  
  var debit_balance=parseInt(current_balance_el.innerText) - parseInt(entered_amount_el)
    document.getElementById("debit").innerHTML;